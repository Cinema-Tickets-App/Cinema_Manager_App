<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Dark Mode -->
    <item android:drawable="@color/white" android:state_enabled="true" android:theme="@android:style/ThemeOverlay.Material.Dark"/>

    <!-- Light Mode -->
    <item android:drawable="@color/black" android:state_enabled="true" android:theme="@android:style/Theme.Material.Light"/>
</selector>
